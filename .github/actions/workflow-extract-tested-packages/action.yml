name: 'Workflow Extract Tested Packages'
description: 'Extract Invenio packages to test from zenodo-rdm uv.lock'
inputs:
  python_version:
    description: 'Python version to use'
    required: true
  config_resolved:
    description: 'Path to resolved config file'
    required: true
  continue_from_previous:
    description: 'Continue from previous run'
    required: true
  filter_packages:
    description: 'Space-separated list of packages to filter'
    required: false
outputs:
  packages:
    description: 'JSON array of packages to test'
    value: ${{ steps.extract.outputs.packages }}
  done_packages:
    description: 'JSON array of already tested packages'
    value: ${{ steps.extract.outputs.done_packages }}
  previous_report:
    description: 'Path to previous report directory'
    value: ${{ steps.extract.outputs.previous_report }}
runs:
  using: 'composite'
  steps:
    - name: Download modified config
      uses: actions/download-artifact@v4
      with:
        name: resolved-config
        path: .

    - name: Setup Python and UV
      uses: ./.github/actions/action-install-python
      with:
        python-version: ${{ inputs.python_version }}

    - name: Clone Zenodo RDM repository
      shell: bash
      run: |
        echo "::group::Cloning zenodo-rdm repository"
        git clone --depth 1 https://github.com/zenodo/zenodo-rdm.git
        echo "::endgroup::"
        echo "✓ zenodo-rdm repository cloned"

    - name: Extract Invenio packages from uv.lock
      id: extract
      shell: bash
      run: |
        # Determine if we should continue from previous run
        opts=()

        if [ "${{ inputs.continue_from_previous }}" = "true" ]; then
          # Find the most recent report directory
          PREVIOUS_REPORT=$(ls -d reports/results/*/ 2>/dev/null | sort -r | head -n 1 || echo "")

          if [ -n "$PREVIOUS_REPORT" ]; then
            echo "Continuing from previous report: $PREVIOUS_REPORT"
            opts+=("--previous-report" "$PREVIOUS_REPORT")
          fi
        fi

        # Add package filter if provided
        if [ -n "${{ inputs.filter_packages }}" ]; then
          echo "Filtering packages: ${{ inputs.filter_packages }}"
          opts+=("--filter-packages" ${{ inputs.filter_packages }})
        fi

        $TOOLS_PYTHON scripts/extract_packages_to_test.py zenodo-rdm/uv.lock "${{ inputs.config_resolved }}" packages.sh ${opts[@]}

        # Source the output variables
        source packages.sh

        # packages is already a JSON array
        echo "Found packages: $packages"
        echo "Already tested packages: $done_packages"
        echo "packages=$packages" >> $GITHUB_OUTPUT
        echo "done_packages=$done_packages" >> $GITHUB_OUTPUT
        echo "previous_report=$PREVIOUS_REPORT" >> $GITHUB_OUTPUT
